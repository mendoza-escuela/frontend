# nginx interno del contenedor del frontend: sirve la SPA compilada.
#
# Escucha en 8080 y no en 80 porque la imagen corre como usuario sin
# privilegios (nginxinc/nginx-unprivileged): un proceso no-root no puede
# abrir puertos por debajo de 1024. Ver hallazgo H-07.
#
# CABECERAS DE SEGURIDAD: se emiten acá y no sólo en el reverse proxy de
# producción. Es defensa en profundidad: si el proxy se reconfigura mal o la
# SPA se sirve desde otro lugar, la protección viaja con el contenedor.
# ZAP baseline reportaba 9 advertencias por su ausencia (Fase 9).
#
# Nota sobre nginx: `add_header` dentro de un `location` REEMPLAZA las
# cabeceras heredadas del bloque `server` en lugar de sumarse. Por eso las
# cabeceras de seguridad se repiten en cada location que agrega las suyas.
server {
  listen 8080;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Desde nginx 1.29.3, `merge` evita que una cabecera de cache declarada en un
  # location elimine accidentalmente las cabeceras de seguridad del server.
  add_header_inherit merge;

  # No revelar la versión de nginx (ZAP 10036).
  server_tokens off;

  add_header X-Content-Type-Options "nosniff" always;
  add_header X-Frame-Options "DENY" always;
  add_header Referrer-Policy "strict-origin-when-cross-origin" always;
  add_header Permissions-Policy "camera=(), microphone=(), geolocation=(), interest-cohort=()" always;
  add_header Strict-Transport-Security "max-age=31536000" always;
  add_header Cross-Origin-Opener-Policy "same-origin" always;
  add_header Cross-Origin-Embedder-Policy "require-corp" always;
  add_header Cross-Origin-Resource-Policy "same-origin" always;
  # Los estilos de la aplicación se cargan desde archivos propios. Algunos
  # componentes React/Recharts necesitan atributos style dinámicos, por eso la
  # excepción queda limitada a style-src-attr y no habilita bloques <style>.
  add_header Content-Security-Policy "default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'none'; form-action 'self'; script-src 'self'; style-src 'self'; style-src-attr 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self' __VITE_API_ORIGIN__; media-src 'self' blob:; worker-src 'self' blob:" always;

  # Sonda para el HEALTHCHECK del contenedor y para el orquestador.
  location = /healthz {
    access_log off;
    add_header Content-Type text/plain always;
    return 200 "ok\n";
  }

  # El HTML nunca se cachea: un despliegue nuevo se ve de inmediato.
  location = /index.html {
    add_header Cache-Control "no-store, must-revalidate" always;
    try_files $uri =404;
  }

  # Configuración pública generada al iniciar el contenedor. Nunca se cachea
  # para que un reemplazo del contenedor adopte inmediatamente su entorno.
  location = /runtime-config.js {
    add_header Cache-Control "no-store, must-revalidate" always;
    try_files $uri =404;
  }

  # Los assets de Vite llevan hash en el nombre: cache larga e inmutable.
  location ^~ /assets/ {
    expires 1y;
    add_header Cache-Control "public, max-age=31536000, immutable" always;
    try_files $uri =404;
  }

  location ~* \.(?:css|js|jpg|jpeg|gif|png|svg|ico|webp|avif|woff2)$ {
    expires 7d;
    add_header Cache-Control "public, max-age=604800" always;
    try_files $uri =404;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
